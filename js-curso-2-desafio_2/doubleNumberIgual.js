function multiplier(num1) {
    return alert(`O resultado da multiplicação de ${num1} por ele mesmo é: ${num1 * num1}`);
}

multiplier(parseInt(prompt("Escolha um número para se multiplicar por ele mesmo")));
