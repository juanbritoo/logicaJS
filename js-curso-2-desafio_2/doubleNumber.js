function num(numero) {
   return alert(numero * 2);
}

num(parseInt(prompt("Escolha um número para multiplicar")));

//explicação do código: define uma função chamada 'num', onde ela recebe o parâmetro 'numero'
//essa função ela retorna o valor de um alert que faz a multiplicaação entre o 'numero' x 2
//chamamos a funçõ 'num()' e dentro dela definimos que é um número inteiro e uma caixa de comando para se digitar o número desejado.
